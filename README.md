## Task Finished By Mantas Matelionis Using NextJS:

# How to start project:

1. Open project;
2. Run command `npm i` to install all dependencies;
3. Run command `npm run dev` to start project;
4. Open your browser to see results;
